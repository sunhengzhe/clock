{
  "name": "watch-face-project",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173",
    "start": "vite --host 0.0.0.0 --port 4173",
    "build": "vite build",
    "deploy": "pnpm build && wrangler deploy",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "preview:cloudflare": "pnpm build && wrangler dev --local --port 8787",
    "test": "node --test"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.3.5",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
